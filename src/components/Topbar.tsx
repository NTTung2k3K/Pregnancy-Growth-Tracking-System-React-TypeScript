const Topbar = () => {
  return (
    <div className="fixed w-full bg-topbar bg-cover h-[45px] flex justify-center items-center text-white font-semibold  z-50">
      Journey of love – Being with your baby from the first days!
    </div>
  );
};

export default Topbar;
